# Handoff — sessão 2026-08-21 (sessão 2)

Sessão parada aqui a pedido do usuário (precisou saída, não por falta de token). Ver `HANDOFF_2026-08-21-sessao1.md` pra o handoff da sessão da manhã (Pool multi-chave, Document Intelligence, Auditor de Peças). HEAD atual de `main`: `ed376dd` (commitado E pushado para `origin/main`).

## 1. Bugs corrigidos e em produção nesta sessão — completo

Usuário reportou uma lista de bugs por WhatsApp/chat. Investigados por evidência (código real, não suposição) e corrigidos 4 causas raiz confirmadas — commit `ed376dd`, 133 testes verdes:

| Bug reportado | Causa raiz | Arquivo |
|---|---|---|
| "Limite de 25 análises" aparecia mesmo no plano Pro | `gerarAnaliseIaAction` (análise de IA da ficha) usava `LIMITE_MENSAGENS_FREE` hardcoded — mesma classe de bug já corrigida no chat (commit `21d0175` da sessão 1), mas nunca replicada nesta action | `app/app/fichas/actions.ts` |
| Análise de documento retornava JSON cru do Gemini (`{"error":{"code":503,...}}`) na tela, "fallback não funcionou" | `gerarRespostaEstruturada` só avançava pro próximo modelo da cadeia quando o erro era classificado como QUOTA (429) — um 503 "UNAVAILABLE" (sobrecarga do modelo, não quota) esgotava as 3 retentativas no MESMO modelo e propagava direto, nunca chegando a tentar o modelo de fallback | `lib/ia/chamada-estruturada.ts` |
| (mesmo sintoma acima, em geral) erro de provider aparecendo cru pro usuário | Faltava uma camada que traduzisse erro de provider pra mensagem em PT-BR antes de chegar na UI — criado `mensagemErroIaParaUsuario()` | `lib/ia/erros.ts` (novo helper), aplicado em `lib/analise-documento/analisar.ts`, `lib/analise-documento/comparar.ts`, `lib/analise-processo/analisar.ts`, `lib/auditoria-peca/auditar.ts` |
| Perguntado "qual modelo você é", a IA respondia "GPT-4 da OpenAI" | Alucinação do modelo sobre a própria identidade (comum em Gemini/Groq quando perguntados isso) — sem instrução no system prompt corrigindo | `lib/ia/system-prompt.ts` |
| Ficha gerada pelo chat saía com 1 frase só, sem detalhe jurídico | Schema da tool `propose_create_ficha`/`propose_update_ficha` não tinha `description` no campo `resumo_fatos` instruindo a IA a escrever narrativa completa (cronologia, fundamentos legais, provas) — Gemini defaultava pro mínimo | `lib/rag/tools.ts` |

**Ação pendente sua:** nenhuma — já em produção (push feito, sem migration nova).

## 2. Fase 5 (Advogado do Contra) e feature de "conta de equipe por escritório" — NÃO iniciadas de fato

Comecei a desenhar as duas em paralelo via `architect` (ADR primeiro, antes de qualquer código), mas o usuário precisou saír no meio e pediu handoff — os dois agentes foram interrompidos ANTES de escreverem qualquer arquivo (confirmado: `docs/adrs/` não tem `0013`/`0014`, `git status` do worktree usado ficou limpo). Nenhum código, nenhum ADR, nenhuma migration desta parte existe ainda. Não é dívida técnica escondida, é escopo que não chegou a começar.

Pontos de partida pra próxima sessão (já mapeados no ADR 0012 e no handoff da sessão 1, item 9):

### Fase 5 — Advogado do Contra
- IA assume perspectiva da parte contrária: argumentos contrários, fragilidades, contradições, precedentes contrários PROVÁVEIS (nunca como citação verificada — pesquisa jurídica com fonte real só na Fase 7), pontos que precisam de prova, perguntas difíceis, recomendações de fortalecimento.
- Decidir: tabela nova ou reuso de `auditorias_peca` (arquitetura recomenda tabela nova — sem notas 0-10, schema de achados adversariais é bem diferente do Auditor).
- Entrada: colar texto / upload (padrão Auditor de Peças) e/ou selecionar tese já cadastrada em `teses_caso` (Fase 1).
- Nova feature key em `FEATURES_PREMIUM` (`lib/planos/gating.ts`).
- Entrar em `lib/ia/limite-concorrencia.ts#existeProcessamentoIaEmAndamento` assim que a tabela existir.
- Guardrail de humildade epistêmica equivalente ao do Auditor — schema Zod tem que forçar "isto é hipótese da IA", não só instrução de prompt.
- Onda de implementação igual às Fases 3/4: `architect` (ADR) → `database` → `ai-engineer` → `senior-engineer` → revisão `security`+`qa`+`techlead`.

### Conta de equipe por escritório (pedido novo do usuário nesta sessão)
Problema real: hoje só existe UM jeito de "entrar pra equipe" — a pessoa se cadastra sozinha em `/cadastro`, o que sempre cria um escritório NOVO com ela como owner (`lib/onboarding.ts#criarEscritorioEPerfil`). `convidarAction` (`app/app/equipe/actions.ts`) só mostra uma mensagem "ainda não implementado, peça pra pessoa se cadastrar direto" — nunca move ninguém pro MESMO escritório. Ou seja: convite de equipe real nunca existiu.

O usuário quer: owner/admin do escritório poder criar a conta do funcionário direto (ou convidar por e-mail), sem depender de auto-cadastro, com separação clara e confidencial entre conversas de clientes/casos diferentes (isso já existe via RLS por `escritorio_id` — validar que não há brecha antes de expandir).

Decisões a tomar na próxima sessão (nenhuma foi fechada ainda):
1. Convite por e-mail real (`supabase.auth.admin.inviteUserByEmail`, ver `lib/supabase/admin.ts`) vs. admin cria a conta direto com senha temporária — checar se já existe provedor de e-mail transacional configurado antes de decidir (se não tiver, "criar conta direto" é o caminho de menor esforço pra entregar rápido).
2. Schema: tabela `convites_equipe` (token/email/escritorio_id/role/expiração/status) só é necessária se for fluxo de convite assíncrono; se for "admin cria a conta direto", não precisa.
3. Impedir que a pessoa convidada crie escritório próprio por engano se cadastrando direto tendo convite pendente pro mesmo e-mail.
4. Teto de nº de membros por plano (hoje não existe) — decidir se entra nesta feature ou fica pra depois.
5. RBAC: reusar `autorizarGestaoDeEquipe` (`app/app/equipe/actions.ts`) já existente.
6. Onda: `architect` (ADR) → `database` → `senior-engineer` (auth admin + actions/UI) → revisão `security` (prioridade alta aqui — é RBAC/convite cross-tenant) + `qa` + `techlead`.

## 3. RAG do copiloto — pedido novo do usuário, não iniciado

Usuário quer que a IA do chat fique "mais esperta, responda melhor" — feedback qualitativo, sem bug específico apontado ainda. Antes de tocar em código, próxima sessão deveria:
- Ler `lib/rag/retrieval.ts`, `lib/rag/embeddings.ts`, `lib/rag/indexacao-interna.ts`, `lib/rag/jurisprudencia.ts` pra mapear o pipeline atual (o que é indexado, tamanho de chunk, k de recuperação, reranking se houver).
- Pedir ao usuário exemplo(s) concreto(s) de resposta ruim antes de mudar qualquer coisa — "responder melhor" sem exemplo é chute; risco real de "otimizar" algo que não é o problema.
- Candidatos comuns de melhoria pra avaliar com evidência (não implementar de cabeça): tamanho/overlap de chunk, nº de chunks recuperados (k), reranking, ampliar fontes indexadas (jurisprudência/legislação já tem grounding via busca web no `system-prompt.ts`, confirmar se RAG interno cobre outra coisa — docs do próprio escritório, histórico de casos).

## 4. Ações pendentes SUAS (usuário)

- Nenhuma migration nova pendente desta sessão (os 4 fixes de bug não tocaram banco).
- Migrations pendentes da sessão 1 que talvez ainda não tenham rodado — confirmar no Supabase: `0033`, `0034` (Document Intelligence), `0035` (Auditor de Peças), `0036` (varchar do Autentique).
- Testar em produção os 4 bugs corrigidos: análise de IA da ficha no plano Pro, análise de documento durante instabilidade do Gemini (deve trocar de modelo agora em vez de mostrar JSON cru), perguntar "qual modelo você é" no chat, gerar uma ficha nova pelo chat (deve vir com texto mais completo).
- Pra próxima sessão: já ter em mãos (1) decisão convite-por-e-mail vs criar-conta-direto pra equipe, e se já existe provedor de e-mail configurado; (2) 1-2 exemplo(s) concreto(s) de resposta do chat que você achou fraca, pra guiar a melhoria de RAG com evidência.

## 5. Notas de processo desta sessão

- **Confirmado em tempo real** (reflog): outra sessão está usando o MESMO working tree principal (`juridico-ia-saas/`) e troca de branch nele enquanto esta sessão roda — exatamente o risco que a sessão 1 já tinha registrado no item 10 do handoff anterior. Toda esta sessão (fixes de bug + tentativa de Fase 5) rodou em worktrees isolados (`git worktree add`) pra nunca disputar o índice/HEAD com a outra sessão. Recomendo manter esse padrão até a outra sessão de landing page terminar.
- Antes de corrigir, cada bug foi confirmado lendo o código real (não suposição) — ex: o bug do limite Pro só foi achado comparando o diff do commit que corrigiu o chat (`21d0175`) contra o código atual de `fichas/actions.ts`, que nunca recebeu o mesmo fix.
- Os 2 ADRs (Fase 5 e conta de equipe) foram interrompidos a pedido do usuário antes de escreverem qualquer arquivo — confirmado que não sobrou nada pela metade no worktree.
