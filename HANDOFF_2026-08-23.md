# Handoff — sessão 2026-08-23 (sessão 5 — retomada pós-travamento + Redesign Landing v3 "papel-e-tinta")

Contexto: a sessão anterior travou DEPOIS de commitar Fases 8–9 (`a3a9384`, `106472f`, `550d923`) e no MEIO do início do redesign da landing (WIP não commitado: tokens/fundação em `globals.css` + fontes/SEO em `layout.tsx` + `icons.tsx` mínimo). O WIP deixou o repo **não compilando** (`tsc` quebrava: `nav/hero/features/pricing/footer` importavam ícones removidos). Esta sessão retomou dali.

## TL;DR

**Landing v3 "papel-e-tinta" COMPLETA e verificada na `staging`**: tsc limpo em todo o projeto, eslint limpo, **476 testes verdes**, `next build` de produção OK, scan anti-AI-slop com ZERO tells. Direção: editorial claro (papel `#faf9f5` / tinta `#141412` / acento único vermelho-lacre `#8b2e1f`; Fraunces + Instrument Sans + IBM Plex Mono), narrativa de 16 seções com assinatura visual "Fio do Caso" (linha em tinta que se desenha no scroll com nós CASO→DOCUMENTO→ANÁLISE→ESTRATÉGIA→TAREFA→AÇÃO).

## O que foi feito nesta sessão

1. **Spec de coordenação** (`docs/redesign-landing-v3.md`): contrato visual/copy usado por todos os agentes — tokens, tipografia, gramática de layout assimétrico, motion (reduced-motion sempre), copy OFICIAL literal e verdades do produto.
2. **Primitivo compartilhado** `section.tsx`: `<Section>` server component (kicker numerado à esquerda, H2 serif dominante à direita, variantes papel/papel-2/tinta).
3. **Onda UI base**: `nav.tsx` (scroll transform, menu mobile acessível, skip-link), `hero.tsx` + NOVO `hero-case.tsx` (CASO 0241 construído em loop silencioso GSAP ~12s com ticker "Documento recebido → Analisando → 3 pontos → 1 prazo → Tarefa criada", chip lacre sincronizado, reduced-motion estático), `footer.tsx`, `faq.tsx` (details nativos, zero JS; FAQ auditado contra verdades — removidas promessas falsas de export DOCX/PDF).
4. **Onda motion**: `silver-thread.tsx` reestilizado para o fio em tinta (sem glow/blobs) + 6 nós lacre posicionados por **bissecção sobre getPointAtLength** (exatamente sobre a curva, rebuild no resize); `text-reveal.tsx` verificado theme-neutral; deletados `light-beam.tsx` (após remover consumidores) e `progress-line.tsx`.
5. **Onda núcleo**: `problem-section.tsx` (caos tipográfico → ordem via CSS scroll-driven com fallbacks em camadas), `case-section.tsx` (#produto), `documents-section.tsx` (contrato anotado ①②③), `audit-section.tsx` (✓/! nunca só cor).
6. **Onda inteligência**: `devil-section.tsx` (tese × ataque adversário), `workflow-section.tsx` (#como-funciona, stepper 8 passos preenchendo no scroll via CSS scroll-driven), `proactive-section.tsx` (painel tinta "BOA TARDE." com números lacre-bright), `research-section.tsx` (#recursos, fonte verificável + bloco de honestidade "Não foi localizada fonte verificável suficiente").
7. **Onda cliente**: `client-section.tsx`, `whatsapp-section.tsx` (nota honesta: aprovação do advogado), `memory-section.tsx`, `result-section.tsx` (ANTES riscado → DEPOIS sólido, zero métricas inventadas), `security-section.tsx`.
8. **Integração (orquestrador)**: `pricing.tsx` reescrito (Free R$0 / Pro R$149 / Firm "Em breve"; REMOVIDAS promessas falsas do antigo: export DOCX/PDF, API abertas), `cta-final.tsx` ("Deixe o sistema cuidar do resto." + cláusula final honesta), `app/page.tsx` (16 seções + `.marketing-root` + JSON-LD SoftwareApplication), deletados features/features-scroller/how-it-works/light-beam.
9. **Fix herdado do WIP**: interface vazia em `icons.tsx` → type alias (lint).

## Verificação (gates)

| Gate | Resultado |
|---|---|
| `tsc --noEmit` | ✅ zero erros no projeto inteiro |
| `eslint` marketing+page | ✅ limpo |
| `npm test` | ✅ 476/476 (era 441 na sessão 4) |
| `next build` produção | ✅ (único warning pré-existente: middleware→proxy deprecado no Next 16, não relacionado) |
| anti-AI-slop | ✅ zero tells (sem gradiente/glow/rounded-2xl/Inter/copy clichê; únicos matches eram comentários proibitivos) |

## Decisões técnicas

- **Coordenação por artefato**: spec em disco + posse exclusiva de arquivos por agente paralelo = zero conflito de escrita; integração centralizada no orquestrador.
- **CSS scroll-driven antes de GSAP** onde não precisa de geometria recalculada (problema/workflow), GSAP só onde há loop/timeline real (hero-case, fio) — padrão do repo.
- **Server Components por padrão**; `"use client"` apenas em nav/hero-case/reveal/text-reveal/silver-thread.
- **Honestidade como design**: Firm aparece "Em breve" sem botão falso; jurisprudência da landing é rotulada ilustrativa; WhatsApp cita aprovação humana; auditoria diz ser ferramenta auxiliar.
- `themeColor` do navegador continua navy (global); a landing clara convive (comentário no layout).

## Pendências (fora desta sessão)

1. **USUÁRIO: rodar migrations 0037–0044 no Supabase** (crítico desde a sessão 4 — ver docs/MIGRATIONS_PENDENTES.md; 0044 workflows entrou na sessão passada).
2. USUÁRIO: redirect URL `/auth/callback` no Supabase + primeiro sync STJ manual.
3. Roadmap restante do prompt mestre: Fase 15 (áudio), 17 (memória do escritório além da atual), 18 (orquestração de agentes), 22 (Firm), 27 (observabilidade de custo IA por token), 28/29 (perf/testes E2E).
4. Testar a landing visualmente (desktop/tablet/mobile) e validar direção — esta segue EXATAMENTE o briefing de redesign aprovado (editorial claro, sem estética "IA").

## Achado importante: linha paralela em origin/staging (RESOLVIDO)

Ao pushar, descobriu-se que `origin/staging` tinha 5 commits de uma sessão paralela de 21/08 pela manhã (`953d4f3`, `ba7fa1e`, `a2d8fb6`, `f785919`, `7b81650`) que a linha local nunca puxara: continha OUTRAS DUAS tentativas de redesign de landing + um cluster próprio de Document Intelligence + pool de chaves (este já presente no local via main). Merge `f35ae87` resolvido com política explícita:
- **Landing**: v3 papel-e-tinta desta sessão (mais nova e conforme briefing) prevaleceu; componentes órfãos da linha remota (hero-case-engine, hero-interactive-studio, section-* etc.) removidos.
- **IA núcleo** (gemini/groq/provider/chamada-estruturada/erros): versões locais (evoluídas nas sessões 2–4 com fixes de fallback/streaming/trivialidade).
- **Doc Intelligence**: UI local mantida; cluster lib/document-intelligence da linha remota veio limpo e os 19 testes dele PASSARAM contra nossa infra — mantidos.
- Pós-merge verificado: tsc limpo, 495 testes verdes, next build OK. staging == origin/staging (push feito).

---

# Sessão 6 (mesmo dia) — conclusão do WIP das Fases 17/18/27 + Fase 15 (áudio)

Contexto: esta sessão partiu de um WIP NÃO COMMITADO deixado pela sessão anterior
(fases 17/18/27 parcialmente implementadas: lib + migrations + páginas criadas, porém
SEM conexão com a IA e SEM instrumentação completa). Diagnóstico inicial: tsc limpo,
543 testes verdes — o WIP era funcional mas inerte.

## TL;DR

**WIP concluído e conectado + Fase 15 entregue.** Suíte terminou em **577 testes**
(543 → 577), tsc/eslint limpos. Migrations **0045–0046** novas (ver
`docs/MIGRATIONS_PENDENTES.md`). Commitado na `staging`.

## O que foi feito (3 frentes em paralelo + integração)

1. **Fase 17 COMPLETA (memória do escritório)**: a memória agora CHEGA na IA.
   `comporSystemInstruction()` nova em `lib/ia/gemini.ts` (pura, compartilhada):
   compõe `SYSTEM_PROMPT → bloco da memória → RAG_TOOLING_PROMPT`; `systemPromptOverride`
   tem precedência; bloco vazio = comportamento idêntico ao anterior. Gemini E Groq
   (one-shot + stream) usam a mesma função; provider propaga; chat streaming e one-shot
   carregam a memória em paralelo (Promise.all) e injetam o bloco. `montarSystemPrompt`
   removida (dead code — substituída por comporSystemInstruction, comentário explica).
2. **Fase 27 COMPLETA (observabilidade de uso)**: `RespostaIa.modelo?: string` preenchido
   com o modelo REAL que respondeu (variável da cadeia de fallback); chat streaming +
   one-shot gravam `origem:"chat"`, `duracao_ms`, `modelo`; as 13 chamadas de IA das
   demais features agora gravam `origem`+`duracao_ms` (analise_processo,
   estrategia_caso, redacao_peca, auditor_peca ×2, advogado_contra ×3, documentos ×2,
   documentos_comparacao, redline, calculadora). A página `/app/uso` passa a mostrar
   latência média e custo por modelo reais, não só contagem.
3. **Fase 15 NOVA (entrada por áudio no chat)**: botão de microfone no composer
   (`chat-app.tsx`) → MediaRecorder → POST `/api/audio/transcrever` (auth obrigatória,
   validação mimetype/tamanho ≤20MB) → Groq `whisper-large-v3-turbo`, language pt →
   TEXTO volta preenchido no textarea para revisão/envio manual (HITL, zero submit
   automático). Transcrição registra uso (`origem:"audio_transcricao"`). Erros honestos:
   chave ausente/permissão negada/navegador sem suporte têm mensagem clara. 28 testes.
4. **Fase 18 (orquestração leve)**: já estava feita no WIP (heurística pura
   `lib/ia/orquestrador.ts` alimentando sugestões do Command Center quando a busca vem
   vazia) — apenas validada nesta sessão.
5. **Limpeza**: `components/marketing/trust-strip.tsx` DELETADO — órfão com estética da
   landing v2 REJEITADA (navy/dourado), fora da spec v3 papel-e-tinta e sem keyframe
   `marquee` no CSS. Decisão: componentes fora do contrato visual aprovado não entram.

## Verificação (gates)

| Gate | Resultado |
|---|---|
| `tsc --noEmit` | ✅ zero erros |
| `eslint .` | ✅ zero erros (8 warnings pré-existentes) |
| `npm test` | ✅ 577/577 (62 arquivos) |
| `next build` produção | ✅ (warning pré-existente middleware→proxy do Next 16) |

## Decisões técnicas

- **Bloco de memória entra ENTRE persona e tooling/RAG**: modelo trata como contexto,
  não como última instrução — razão de existir de `comporSystemInstruction`.
- **Mensagens triviais também recebem o bloco** se configurado (~0 tokens extras,
  query paralela): consistência de tom vale desde o "bom dia".
- **Transcrição consome quota mensal** (linha em uso_ia): decisão de produto — reverter
  é filtrar por `origem` na contagem, trivial.
- **Rótulos de origem estáveis** viram dimensão analítica da página /app/uso — não
  renomear sem migrar linhas antigas.

## Pendências atualizadas

Ver `PENDENCIAS.md` reescrito nesta sessão (estado real consolidado): migrations
0037–0046 (crítico), redirect URL, primeiro sync STJ, env vars Stripe/Groq/etc.,
validação visual da landing v3 e do ditado, e o roadmap restante com critério de
desbloqueio (Fase 22 Firm = decisão de negócio; 28 perf = medir primeiro; 29 E2E =
ambiente com RAM).

---

# Sessão 7 (mesmo dia) — Paleta unificada papel-e-tinta (landing + app) + Fases 22/28/29

Pedido do dono: outra cor na landing (o vermelho-lacre não agradou), MESMO estilo/cor
dentro do app, fechar as fases restantes, e integridade total para o merge staging→main.

## TL;DR

**Produto inteiro unificado no papel-e-tinta com NOVO acento VERDE-SELO (#1d5b46)** +
webhooks reais (Fase 22 técnica) + perf + E2E Playwright. ADR **0016** documenta a
decisão. Migrations **0045–0047** pendentes de aplicação.

## O que foi feito

1. **Fundação (orquestrador, globals.css + layout.tsx)**: variáveis históricas do app
   (`navy/ice/silver/muted/green`) REMAPEADAS por valor para a paleta clara — os ~140
   arquivos que usam esses tokens migraram sem tocar neles; tokens da landing renomeados
   `lacre→accent` com hex verde; fontes unificadas: Fraunces assume `--font-display`
   (era Playfair) e Instrument Sans assume `--font-sans` (era Inter), Playfair+Inter
   REMOVIDOS do carregamento; themeColor → papel.
2. **6 frentes visuais paralelas** (partições disjuntas, ~100 arquivos): ui primitives,
   shell/sidebar/command-center/login, núcleo escritório (dashboard/radar/prazos/uso/
   relatórios/equipe), chat/portal/cadastro/triagem, ferramentas IA (auditor/contra/
   pesquisa/documentos/redline/comparador), fichas/workflows/propostas/admin/shell/
   calculadoras/donut. Conversões: white/x → ink/x, sombras duras → suaves, status
   -300/-400 → 700/800 AA sobre papel, banners -950 → -50/-200, citações/trechos em
   nota editorial border-l accent, timeline/workflow editor no novo idioma visual.
   Única exceção estrutural autorizada: dynamic import do CommandCenter no app-shell
   (perf — sai do bundle inicial).
3. **Fase 22 técnica (webhooks)**: migration 0047 (`webhook_endpoints` +
   `webhook_deliveries`, RLS padrão 0044), signer HMAC-SHA256 (`t=<ts>,v1=<hmac>`,
   timing-safe), validação SSRF sintática, backoff exponencial puro testável, página
   `/app/integracoes` gated Pro (api_integracoes) com CRUD + últimas deliveries,
   emissor central best-effort INTEGRADO nos gatilhos reais: `criarPrazoAction`
   (prazo.criado) e `aprovarPropostaAction` (prazo.criado/atualizado, caso.criado/
   atualizado). 22 testes novos. Billing Firm segue bloqueado por decisão de negócio.
4. **Fase 29 (E2E Playwright)**: @playwright/test 1.62 + chromium instalados,
   playwright.config (porta 3100, chromium only), 3 specs smoke (landing h1 sem erro
   de console, auth redirect via middleware, manifest 200). Script `test:e2e`.
5. **Fase 28 (perf)**: fonts mortas removidas (Playfair/Inter), CommandCenter fora do
   bundle inicial, tabular-nums nos números grandes.

## Verificação

| Gate | Resultado |
|---|---|
| tsc --noEmit | ✅ zero erros |
| eslint . | ✅ zero erros |
| vitest | ✅ 599/599 |
| next build produção | ✅ |
| Resíduos white/black fora da landing | ✅ zero |

## Pendências atualizadas

- USUÁRIO: rodar migrations 0037–**0047** (docs/MIGRATIONS_PENDENTES.md).
- USUÁRIO: validar visualmente landing v3.1 (verde) e o APP interno claro (todas as telas).
- Cron de retry das webhook_deliveries pendentes (usa calcularProximaTentativa já pronto)
  e mais gatilhos de evento se o produto pedir — documentado nos JSDocs.
- Billing Firm: decisão de preço/Stripe segue com o dono.

---

# Auditoria pós-merge via CLI Vercel + Supabase REST (mesmo dia)

## Deploy confirmado
- Merge `5fcdcbc` pushado na main; deploy Production **● Ready** em 45s.
- Produção ao vivo: `/`, `/login`, `/manifest.webmanifest` → 200.

## Env vars (vercel env ls production)
Completas: trio Stripe INTEIRO (causa do bug "não leva pro checkout" resolvida),
GEMINI_API_KEY, GROQ_API_KEY (ditado habilitado), SUPABASE_SERVICE_ROLE_KEY,
CRON_SECRET, AUTENTIQUE_API_TOKEN, chaves de criptografia IA/WhatsApp.
Ausentes (conhecidas): AUTENTIQUE_WEBHOOK_SECRET (webhook assinatura sem verificação),
DJEN_API_BASE_URL (tem default), DATAJUD_API_KEY (aguardando credenciamento CNJ).

## ⚠️ Migrations — auditoria REAL (via PostgREST, ref bpuslirsfzqpoyzgodgu)
Primeira varredura deu falso positivo (NEXT_PUBLIC_SUPABASE_URL ausente do .env.local
local → curl sem host → grep de erro nunca batia). Corrigido extraindo o ref do JWT
da service key. Estado REAL do banco:
- ✅ Existem: uso_ia (com dados), jurisprudencias (VAZIA — sync STJ nunca rodou),
  tarefas_caso.
- ❌ AUSENTES: convites_equipe (0038/0040), estrategias_caso (0041),
  fontes_stj_sync (0042), workflows (0044), webhook_endpoints/webhook_deliveries (0047);
  colunas modelo/duracao_ms/origem em uso_ia (0045) e diretrizes_ia/tom_escrita/
  clausulas_padrao em escritorios (0046).
→ Equipe, Advogado do Contra, Estrategista, Pesquisa STJ/sync, Workflows, /app/uso
  (colunas novas), Memória do escritório e Webhooks estão DEGRADADOS EM PRODUÇÃO até
  as migrations serem aplicadas. docs/MIGRATIONS_PENDENTES.md tem a ordem.
- CRON_SECRET não existe localmente (só Vercel, encrypted): primeiro sync STJ precisa
  do dono OU espera o cron do dia 3, 04h UTC.

## Caminhos para aplicar (dono decide)
A) Automático: criar access token em supabase.com/dashboard/account/tokens → salvar
   como SUPABASE_ACCESS_TOKEN no .env.local → orquestrador roda `supabase link` +
   `migration repair` + `db push` e re-audita.
B) Manual: colar cada SQL no SQL Editor (padrão das sessões anteriores).
