# Handoff — sessão 2026-08-20

Contexto de janela de tokens curta: sessão parada aqui de propósito, antes de estourar. Tudo abaixo já está **commitado e pushado em `main`** (não staging — decisão explicada no fim). Este arquivo em si é o único commit feito direto em `staging`.

## 1. Bugs corrigidos (produção, `main`)

| Bug relatado | Causa raiz | Commit |
|---|---|---|
| Checkout Stripe não redireciona | `STRIPE_SECRET_KEY`/`STRIPE_PRICE_ID_PRO_MENSAL` ausentes em prod (config, não código) + `customer_email` sempre vazio no código + erros engolidos sem log | `d16f97c` |
| Admin "mostra tudo um grupo só" | Botão ambíguo em `/admin/usuarios` promovia a admin de PLATAFORMA (cross-tenant) em vez de admin do escritório. RLS/isolamento sempre estiveram corretos — era confusão de UX | `4c87896` |
| Troca de conversa não funciona no mobile | Lista de conversas tinha `hidden` fixo abaixo do breakpoint `md`, nunca renderizava no mobile | `b687504` |
| Animação "digitando" travada | Era texto estático sem CSS de animação nenhuma | `b687504` |
| "IA indisponível" intermitente | Investigado — **não é bug de código**, é 429/5xx transiente do Gemini, já tratado com retry+fallback Groq. Não alterado. | — |

**Ação pendente sua**: preencher `STRIPE_SECRET_KEY`/`STRIPE_PRICE_ID_PRO_MENSAL`/`STRIPE_WEBHOOK_SECRET` na Vercel + redeploy manual. Passo a passo em `.env.example`. Também: checar tabela `plataforma_admins` no Supabase e remover manualmente qualquer usuário promovido por engano pelo bug do botão ambíguo (o código não corrige isso retroativamente).

## 2. Auditoria Fase 0 (feita, não gerou commit — só relatório na conversa)

Achados sem correção ainda (nenhum crítico):
- `middleware.ts` não protege `/admin` no matcher — depende só do guard em `app/admin/layout.tsx`. Funciona, mas sem defesa em profundidade.
- `MODELO_PRO` = `MODELO_FLASH` no Gemini (limitação de quota atual, documentado em código).
- Rate limit em memória, não serverless-safe (risco pra triagem pública/consulta CPF).
- Upload valida por extensão/MIME declarado, não conteúdo binário real (`base-conhecimento/actions.ts` e a nova feature de análise de processo têm o mesmo padrão).

## 3. Fase 1 — Caso Inteligente (completa, `main`)

Schema: migrations `0022`-`0029` (soft-delete de ficha + `pessoas_caso`, `eventos_caso`, `teses_caso`, `caso_jurisprudencia_citada`, `tarefas_caso`, `memoria_ia_caso`).
Backend: `lib/casos/{pessoas,timeline,teses,tarefas,memoria-ia}.ts` + actions, hooks automáticos de timeline em prazos/petições/assinatura.
UI: `app/app/fichas/[id]/page.tsx` ganhou abas (Visão Geral / Pessoas / Linha do Tempo / Tarefas / Teses).

## 4. Fase 2 — Análise Inteligente de Processos (completa, `main`)

Upload de PDF/DOCX/imagem vinculado a uma ficha → IA extrai e produz 12 seções estruturadas (resumo, timeline, partes, questões jurídicas, teses, evidências, contradições, lacunas, riscos, prazos, próximas ações, perguntas a investigar), cada item com `trechoOriginal`/`pagina`/`certeza` (nunca inventa fato — marca `nao_encontrado` explicitamente).

- ADR: `docs/adrs/0004-analise-inteligente-processos.md`.
- Schema: `analises_processo` (migrations `0030`, `0031`).
- IA: `lib/analise-processo/{extracao,prompt,analisar}.ts`.
- Action+UI: `app/app/fichas/[id]/analise-processo-{actions,section}.tsx`, `lib/analise-processo/writeback.ts`.
- Write-back: pessoas/timeline/teses são aditivos automáticos; **prazos exigem aprovação humana** via `propostas_acao` (mesmo padrão do chat) — decisão de segurança jurídica, não escreve prazo sozinho.
- Revisão (security+qa+techlead) feita: sem bloqueante. 2 fixes reais já aplicados (prazo perdia vínculo com a ficha; função de tese estava no módulo errado) — commit `acf5609`.
- Gating: feature `analise_inteligente_processo` é Pro-only (`lib/planos/gating.ts`).

**Ação pendente sua**: rodar migrations `0022` a `0031` no Supabase real (SQL editor ou CLI) — sem isso essas abas/feature ficam vazias/com erro tratado (não quebra a página, mas não funciona de verdade).

**Débito técnico registrado, não bloqueante, para revisitar quando houver tempo**:
- `lib/analise-processo/analisar.ts` duplica lógica de retry/fallback de quota que já existe em `lib/ia/gemini.ts` (o ADR original previa extrair um helper compartilhado, não aconteceu — decisão do agente da onda, registrada em comentário no código).
- Guardrail anti-alucinação valida só que `trechoOriginal` não está vazio, não que ele de fato ocorre no texto do documento (fuzzy-match seria mais forte, mitigado hoje por prompt + revisão humana antes do "Aplicar ao caso").
- `FieldError` (`components/ui/input.tsx`, componente compartilhado, afeta vários formulários do app) não tem `aria-live`/`role="alert"` — achado de acessibilidade, fora do escopo desta feature.

## 5. Estado dos testes/build

`npx eslint` e `npx next build` limpos em todo commit desta sessão. `npx vitest run` → 208/208 passando no último commit (`acf5609`).

## 6. Próximo passo — Fase 3: Document Intelligence

Ainda não iniciada. Do roadmap original (`CLAUDE.md`/prompt mestre): resumir/explicar/extrair informação de documento individual e em lote, comparador de documentos (Contrato A × Contrato B: cláusulas adicionadas/removidas, mudanças relevantes, riscos, recomendações), classificação de documento. Pontos de partida reaproveitáveis já existentes no código: `app/app/redline/**` (análise de risco contratual cláusula-por-cláusula já tem boa parte do padrão de prompt/parse pra comparação estruturada), `lib/analise-processo/extracao.ts` (extração PDF/DOCX já pronta e reaproveitável), `app/app/base-conhecimento/actions.ts` (upload em lote já existe pra RAG, pode servir de referência pro upload em lote de Document Intelligence).

Sugestão de próxima sessão: repetir o padrão desta — `architect` desenha (ADR) antes de qualquer código, depois `database`+`ai-engineer`+`senior-engineer` em ondas, revisão final `security`+`qa`+techlead (usar `general-purpose` com prompt de tech lead — o tipo `techlead` não existe no registry de agentes deste ambiente, só existe como conceito no `CLAUDE.md`).

## 7. Notas de processo desta sessão

- Todo trabalho foi feito direto em `main` (não `staging`) — o `PENDENCIAS.md` antigo dizia pra usar `staging`, mas o histórico de commits mostrou que o fluxo real já migrou pra commitar direto em `main` a cada fix/feature pequena, então segui o padrão atual em vez do documento desatualizado. Só este handoff foi commitado em `staging`, a pedido explícito do usuário nesta sessão.
- Padrão usado: fan-out de agentes paralelos por domínio/arquivo disjunto, dispatch sequencial só quando havia dependência real (schema antes de backend, backend antes de UI, tudo antes de revisão).
- Cada agente validou `eslint`+`next build`(+`vitest`) e comitou/pushou sozinho ao terminar — nenhum lote grande esperando tudo ficar pronto.
